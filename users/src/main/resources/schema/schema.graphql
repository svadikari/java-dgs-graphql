type Query {
users: [User]
    user(id: ID!): User
}

type Mutation {
    createUser(user: CreateUser): User
}

input CreateUser {
    name: String!
}

type User {
    id: ID!
    name: String!
    books: [Book]
}

type Book @key(fields: "id") {
    id: ID!
}
